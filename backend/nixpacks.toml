[phases.build]
cmds = ["pip install -r requirements.txt", "python manage.py collectstatic --noinput"]

[phases.start]
cmd = "python manage.py migrate && gunicorn outreach_backend.wsgi --bind 0.0.0.0:$PORT"
